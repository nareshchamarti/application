pipeline {
    agent any

    stages {
        stage('Clone') {
            steps {
                git 'https://github.com/nareshchamarti/application.git'
            }
        }

        stage('Build') {
            steps {
                echo 'Building the project...'
                // Add your build commands, e.g., mvn clean install
            }
        }

        stage('Test') {
            steps {
                echo 'Running tests...'
                // Add test commands, e.g., mvn test
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploying application...'
                // Optional: deploy commands
            }
        }
    }
}
